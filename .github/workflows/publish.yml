name: Publish to Maven Central

on:
  push:
    tags:
      - 'v*'

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Extract version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT

      - name: Build native library
        run: bash build.sh

      - name: Run verification
        run: bash verify.sh

      - name: Generate sources for Maven
        run: |
          mkdir -p src/main/java/supranational/blst
          swig -c++ -java -package supranational.blst \
            -outdir src/main/java/supranational/blst \
            -o /dev/null blst/bindings/blst.swg

      - name: Set up Java and Maven
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'maven'
          server-id: central
          server-username: MAVEN_USERNAME
          server-password: MAVEN_PASSWORD
          gpg-private-key: ${{ secrets.GPG_PRIVATE_KEY }}
          gpg-passphrase: GPG_PASSPHRASE

      - name: Build and verify
        run: mvn -B -ntp -Drevision=${{ steps.version.outputs.VERSION }} -Dgpg.skip=true clean verify

      - name: Publish to Maven Central
        run: mvn -B -ntp -Drevision=${{ steps.version.outputs.VERSION }} deploy -DskipTests
        env:
          MAVEN_USERNAME: ${{ secrets.OSSRH_USERNAME }}
          MAVEN_PASSWORD: ${{ secrets.OSSRH_TOKEN }}
          GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
